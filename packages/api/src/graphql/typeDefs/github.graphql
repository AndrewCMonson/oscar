# Overridden by codegen type "Repository"
type Repository {
  name: String
  description: String
  url: String
}

# Overridden by codegen type "CreatedNewIssue"
type CreatedNewIssue {
  title: String
}

# overridden by codegen type "Respositories"
type Repositories {
  repositories: [Repository]
}

# Overridden by codegen type "CreatedNewRepository"
type CreatedNewRepository {
  repositoryName: String
}

type Query {
  getRepository(repositoryName: String!): Repository
  getRepositories: Repositories
}
type Mutation {
  createNewRepository(repositoryName: String!): CreatedNewRepository
  createNewIssue(
    repositoryName: String!
    issueTitle: String!
    issueBody: String!
  ): CreatedNewIssue
}
